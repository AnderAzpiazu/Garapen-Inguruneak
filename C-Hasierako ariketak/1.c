#include <stdio.h>
int main() {
    int i, j, iso=1;
    for (i=0; i<arrayLuzeera-1; i++) {
        for (j=i+1, j<arrayLuzeera, j++) {
            if(srt[i] == str[j])
            iso=0;
            break;
        }
    }
}
